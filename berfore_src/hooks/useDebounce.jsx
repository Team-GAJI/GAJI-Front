// import { useCallback } from "react";

//     // debounce 함수를 사용하여 디바운싱된 함수 생성
// export const useDebounce = useCallback(debounce((text) => {
//         getSearchMovies(text);
//         console.log('Input Value:', text);
//     }, 500), []);